type randomFn = () => number;

export const generateRandomValue : randomFn = () => {
    return Math.floor(Math.random() * 1000); 
  }

export  const randomPoz1 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz2 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz3 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz4 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
}

export  const randomPoz5: React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz6 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz7 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz8 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
}

export  const randomPoz9 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz10 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz11 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
  }

export  const randomPoz12 : React.CSSProperties = {
    top: generateRandomValue(),
    left: generateRandomValue(),
}